$schema: "http://json-schema.org/draft-04/schema#"

description: an entry in the target sample sheet
properties:
  sample:
    type: string
    description: sample name/identifier

required:
  - snpArraySample